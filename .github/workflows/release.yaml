name: Release cansat-edu-lib

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Build cansat-edu-lib.zip
        run: ./tools/build_release.sh

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" dist/cansat-edu-lib.zip \
            --title "${{ github.ref_name }}" \
            --generate-notes
